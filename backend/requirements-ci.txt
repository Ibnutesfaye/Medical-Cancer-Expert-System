# Lightweight dependencies for the isolated backend unit-test job.
# Production/ML dependencies remain in requirements.txt and the Docker image.
PyPDF2==3.0.1
pdfplumber==0.11.0
pydantic==2.10.5
reportlab==4.2.5
pytest==8.3.4
pytest-timeout==2.3.1
